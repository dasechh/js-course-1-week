const main = () => { // Объявление функции main
    let username = prompt('Введите имя пользователя'); // Запрос имени пользователя через prompt
    return alert('Привет, ' + username + '!') // Вывод приветствия через alert
}

main() // Запуск функции main 